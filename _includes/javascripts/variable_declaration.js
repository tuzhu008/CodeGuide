function doSomethingWithItems(items) {
    // use multiple var
    var value = 10;
    var result = value + 10;
    var i;
    var len;

    for (i = 0, len = items.length; i < len; i++) {
        result += 10;
    }
}
